# URL base da API backend.
# Desenvolvimento: deixe vazio ou apontando pro backend local (o proxy do Vite cobre /api em dev mesmo sem essa variável).
# Produção (Vercel): defina como a URL do backend no Render, ex.: https://sd-project-looptone.onrender.com
VITE_API_BASE_URL=http://localhost:8000
